// Placeholder function. See svelte.config.js for the real implementation
// (essentialy a preprocessor macro). Avoid leaving calls to this function
// around in commits. Should be pretty expensive to call a lot.
export function debug_log(...args: any[]) {
  console.log(args);
}
